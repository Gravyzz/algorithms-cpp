#include <iostream>
#include <vector>

using namespace std;

int n;
vector<bool> visited;
vector<vector<int>> matrix;

void dfs(int v){
	visited[v] = true;
	for (int i = 0; i < n; i++) {
		if (matrix[v][i] == 1 && !visited[i]) {
			dfs(i);
		}
	}
}

int main() {
	int start;

	cin >> n;
	cin >> start;
	start--;

	matrix.assign(n, vector<int>(n));
	visited.assign(n, false);

	for (int i = 0; i < n; i++) {
		for (int j=0;j<n;j++) {
			cin >> matrix[i][j];
		}
	}

	dfs(start);

	for (int i = 0; i < n; i++) {
		if (visited[i]) {
			cout<<i+1<<" ";
		}
	}

	return 0;
}
